{% extends 'base.html.twig' %}

{% block title %}Publisher{% endblock %}

{% block body %}
    <h1>Publisher</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ publisher.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ publisher.designation }}</td>
            </tr>
            <tr>
                <th>CreationDate</th>
                <td>{{ publisher.creationDate ? publisher.creationDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_publisher_index') }}">back to list</a>

    <a href="{{ path('app_publisher_edit', {'id': publisher.id}) }}">edit</a>

    {{ include('publisher/_delete_form.html.twig') }}
{% endblock %}
